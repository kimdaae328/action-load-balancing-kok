<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kok.mapper.AdvertisementClickMapper">
    <!-- 광고 클릭 insert-->
    <insert id="insertAdvertisementClick">
        insert into tbl_advertisement_click(advertisement_id, user_id)
        values (#{advertisementId}, #{userId});
    </insert>
</mapper>